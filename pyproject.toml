[project]
name = "tds"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.139.0",
    "prometheus-client>=0.25.0",
    "uvicorn>=0.50.2",
]

[tool.uv.workspace]
members = [
    "week2/tunnel-demo",
]
